#version 450
layout(local_size_x = 1) in;
layout(set = 0, binding = 0) buffer B { uint x; } b;
void main() {
	// counts to the top of a uint, seconds of gpu time, which Metal reports as a hang
	while (b.x != 0xFFFFFFFFu) { b.x++; }
}
